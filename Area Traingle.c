#include <stdio.h>
int main()
{
int l,h,area;
printf("Enter Length and height \n:");
scanf("%d%d",&l,&h);
area = l * h / 2;
printf("\n The area of triangle is %d and %d is %d \n",l,h,area);
return(0);
}
