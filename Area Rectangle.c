#include <stdio.h>
int main()
{
int l,b,area;
printf("Enter Length and Breath \n:");
scanf("%d%d",&l,&b);
area = l * b;
printf("\n The area of rectangle is %d and %d is %d \n",l,b,area);
return(0);
}
